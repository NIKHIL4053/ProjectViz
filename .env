# ──────────────────────────────────────────────────────────────────────────────
# .env — Environment Variables for Loan Dashboard
# ──────────────────────────────────────────────────────────────────────────────
#
# ! NEVER commit this file to git — add .env to your .gitignore
# ! NEVER share this file — it contains credentials
#
# * HOW TO USE:
# * 1. Copy this file to D:\Loan_Dashboard\.env
# * 2. Fill in values marked TODO
# * 3. Leave everything else as-is — config.py has sensible defaults
#
# ──────────────────────────────────────────────────────────────────────────────


# ── OLLAMA ────────────────────────────────────────────────────────────────────

OLLAMA_BASE_URL=http://localhost:11434
CODER_MODEL=qwen2.5-coder:14b
FAST_MODEL=qwen2.5:7b
OLLAMA_TIMEOUT=120
OLLAMA_TEMPERATURE=0.1
OLLAMA_MAX_RETRIES=2



# ── POSTGRESQL (Docker) ───────────────────────────────────────────────────────

PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=loan_db
PG_USER=postgres
PG_PASSWORD=your_password_here       # TODO: set this

# * Exact schema and table name in your PostgreSQL database
PG_SCHEMA=public
PG_TABLE_NAME=final_table

# * Max rows fetched per SQL query — acts as a safety cap
PG_MAX_ROWS=100000

# * Set to true during development — uses mock data, no DB calls made
USE_MOCK_DATA=false



# ── DATA STORAGE ──────────────────────────────────────────────────────────────

# * Parquet compression — snappy is fastest, gzip is smallest
PARQUET_COMPRESSION=snappy

# * How long (minutes) to keep session parquet files before auto-delete
TEMP_DATA_TTL_MINUTES=60


# ── CHROMADB ──────────────────────────────────────────────────────────────────

CHROMA_TOP_K=10


# ── MODEL CONTEXT ─────────────────────────────────────────────────────────────

SAMPLE_ROWS_FOR_MODEL=5
MAX_CONTEXT_TOKENS=6000


# ── CHARTS ────────────────────────────────────────────────────────────────────

CHART_DPI=150
MAX_CHART_CATEGORIES=15
SCATTER_SAMPLE_ROWS=600
PDF_PAGE_SIZE=A4
PDF_ORIENTATION=landscape


# ── STREAMLIT ─────────────────────────────────────────────────────────────────

MAX_SIDEBAR_SLICERS=5
MAX_CHAT_HISTORY=20


# ── LOGGING ───────────────────────────────────────────────────────────────────

LOG_LEVEL=DEBUG
LOG_ROTATION=10 MB
LOG_RETENTION=5